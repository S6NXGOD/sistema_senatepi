'use client';

/**
 * Mudou para `@/lib/use-tela-larga` em 14/09/2026, quando a Agenda passou a
 * decidir a visão inicial pela mesma largura. Este arquivo fica só para quem
 * ainda importa do caminho antigo (a tela de Escalas) — uma regra, dois
 * endereços, nunca duas cópias.
 */
export { useTelaLarga } from '@/lib/use-tela-larga';
