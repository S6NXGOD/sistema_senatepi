# Dependencies
node_modules/
.pnp
.pnp.js

# Environment (nunca subir segredos)
.env
.env.local
.env.*
!.env.example

# Build outputs
dist/
build/
.next/
# um diretório de build por cliente (ver apps/web/next.config.mjs)
.next-*/
# Gerado pelo Next a cada build e agora APONTA PARA O distDir do cliente
# (`.next-senatepi`, `.next-sindserm`). Versionado, sujaria a árvore a cada
# troca de sindicato e entraria em conflito entre clientes.
apps/web/next-env.d.ts
out/
*.tsbuildinfo

# Railway
.railway/

# Prisma
apps/api/prisma/migrations/dev.db*

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# Coverage / tests
coverage/
.nyc_output/

# Uploads / mídia local (NÃO ignorar código-fonte: use caminhos específicos!)
# Antes havia "storage/" genérico, que ignorava apps/api/src/common/storage/ (código).
/apps/api/uploads/
# um acervo por cliente em dev (ver apps/api/.env.<cliente>)
/apps/api/uploads-*/
/uploads/
/storage_local/
minio-data/
postgres-data/

# Misc
*.pem
.cache/
.turbo/
